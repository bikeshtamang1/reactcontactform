import styles from "./ContactHeader.module.css";
const ContactHeader = () => {
  return (
    <div className={`${styles.contact_section}`}>
      <h1>React</h1>
      <p>
      Contact up page using react
      </p>
    </div>
  );
};

export default ContactHeader;
